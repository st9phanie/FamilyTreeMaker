
const TabLayout = ({ children }: { children: React.ReactNode }) => {

    return (
        <div className='text-primary ml-[360px] p-5 w-full'>
            {children}
        </div>
    )
}

export default TabLayout